<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Итгэмжлэгдсэн proxy
    |--------------------------------------------------------------------------
    |
    | Апп нь Cloudflare, load balancer эсвэл урд талын nginx-ийн АРД ажиллаж
    | байвал бүх хүсэлт proxy-ийн IP-тэй ирнэ. Тэгэхээр $request->ip() нь
    | жинхэнэ үйлчлүүлэгчийн биш, proxy-ийн хаягийг буцаана.
    |
    | Энэ нь CallPro-гийн webhook-д ШУУД нөлөөлнө: VerifyCallProWebhook нь
    | services.callpro.allowed_ips жагсаалттай $request->ip()-г тулгадаг тул
    | үүнийг тохируулахгүй бол БҮХ webhook 403 болж хаагдана.
    |
    | Утга:
    |   null (хоосон) — нэг ч proxy-д итгэхгүй. nginx шууд PHP-FPM рүү
    |                   дамжуулдаг, өөр proxy байхгүй бол ЭНЭ нь зөв.
    |   '*'           — хүсэлтийг дамжуулж буй proxy-д (REMOTE_ADDR) итгэнэ.
    |                   Cloudflare / load balancer-ийн ард байвал үүнийг сонго.
    |   '10.0.0.1,10.0.0.2' — зөвхөн заасан proxy-нуудад итгэнэ (хамгийн нарийн).
    |
    | АНХААР: proxy-гийн ард БАЙХГҮЙ атлаа '*' тавибал X-Forwarded-For-г хэн
    | ч хуурамчаар илгээж IP шүүлтүүрийг тойрч болно. Хэрэггүй бол хоосон үлдээ.
    |
    */

    'proxies' => env('TRUSTED_PROXIES'),

];
